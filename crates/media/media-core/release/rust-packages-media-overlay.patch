diff --git a/Cargo.toml b/Cargo.toml
index 487cfb27..a77333d1 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -53,6 +53,7 @@ members = [
     "crates/image/image-analysis-processing",
     "crates/image/image-analysis-segmentation",
     "crates/image/image-analysis-synthesis",
+    "crates/media/media-core",
     "crates/text/text-analysis-corpus",
     "crates/text/text-analysis-core",
     "crates/text/text-analysis-features",
@@ -145,6 +146,7 @@ candle-core = "0.10.2"
 candle-nn = "0.10.2"
 candle-transformers = "0.10.2"
 image = { version = "0.25", default-features = false, features = ["png", "jpeg", "webp"] }
+media-core = { package = "moenarch-media-core", version = "0.1.0", path = "crates/media/media-core" }
 animation-core = { version = "0.1.0", path = "crates/animation/animation-core" }
 comfyui-data = { version = "0.1.0", path = "crates/comfyui/comfyui-data" }
 comfyui-latents = { version = "0.1.0", path = "crates/comfyui/comfyui-latents" }
diff --git a/crates/video/video-analysis-core/Cargo.toml b/crates/video/video-analysis-core/Cargo.toml
index 07b96a88..02797f5c 100644
--- a/crates/video/video-analysis-core/Cargo.toml
+++ b/crates/video/video-analysis-core/Cargo.toml
@@ -13,6 +13,7 @@ keywords.workspace = true
 categories.workspace = true
 
 [dependencies]
+media-core.workspace = true
 num-rational.workspace = true
 num-traits.workspace = true
 thiserror.workspace = true
diff --git a/crates/video/video-analysis-core/src/lib.rs b/crates/video/video-analysis-core/src/lib.rs
index daec74d4..33608811 100644
--- a/crates/video/video-analysis-core/src/lib.rs
+++ b/crates/video/video-analysis-core/src/lib.rs
@@ -1,5 +1,7 @@
 #![doc = include_str!("../README.md")]
 
+pub use media_core::{AnalysisEvent, Timebase, Timestamp};
+
 use std::collections::{BTreeMap, BTreeSet};
 use std::fmt;
 use std::ops::{Add, Sub};
@@ -55,48 +57,6 @@ pub enum DetectError {
 /// Type alias for result.
 pub type Result<T> = std::result::Result<T, DetectError>;
 
-#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash)]
-/// Data type for timebase.
-pub struct Timebase {
-    /// The num value.
-    pub num: i32,
-    /// The den value.
-    pub den: i32,
-}
-
-impl Timebase {
-    /// Creates a new value.
-    pub const fn new(num: i32, den: i32) -> Self {
-        Self { num, den }
-    }
-
-    /// Returns seconds per tick.
-    pub fn seconds_per_tick(self) -> f64 {
-        self.num as f64 / self.den as f64
-    }
-}
-
-#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash)]
-/// Data type for timestamp.
-pub struct Timestamp {
-    /// The PTS value.
-    pub pts: i64,
-    /// The timebase value.
-    pub timebase: Timebase,
-}
-
-impl Timestamp {
-    /// Creates a new value.
-    pub const fn new(pts: i64, timebase: Timebase) -> Self {
-        Self { pts, timebase }
-    }
-
-    /// Returns seconds.
-    pub fn seconds(self) -> f64 {
-        self.pts as f64 * self.timebase.seconds_per_tick()
-    }
-}
-
 #[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash)]
 /// Data type for frame position.
 pub struct FramePosition {
@@ -940,43 +900,6 @@ pub struct RealtimeVideoAnalysisResult {
     pub frames_processed: u64,
 }
 
-#[derive(Debug, Clone, PartialEq)]
-/// Data type for analysis event.
-pub struct AnalysisEvent {
-    /// Timestamp associated with this value.
-    pub timestamp: Option<Timestamp>,
-    /// The analyzer value.
-    pub analyzer: String,
-    /// Label assigned to this value.
-    pub label: String,
-    /// Score assigned to this value.
-    pub score: Option<f32>,
-}
-
-impl AnalysisEvent {
-    /// Creates a new value.
-    pub fn new(analyzer: impl Into<String>, label: impl Into<String>) -> Self {
-        Self {
-            timestamp: None,
-            analyzer: analyzer.into(),
-            label: label.into(),
-            score: None,
-        }
-    }
-
-    /// Returns at timestamp.
-    pub fn at_timestamp(mut self, timestamp: Timestamp) -> Self {
-        self.timestamp = Some(timestamp);
-        self
-    }
-
-    /// Returns score.
-    pub fn score(mut self, score: f32) -> Self {
-        self.score = Some(score);
-        self
-    }
-}
-
 #[derive(Debug, Default, Clone, PartialEq)]
 /// Data type for audio analysis result.
 pub struct AudioAnalysisResult {
