schema_version = 1
profile_status = "draft"
project_id = "moritzbrantner/moenarch-foundation"
default_base_ref = "HEAD"
maturity = "growing"
project_kinds = ["library", "cli", "native-processing"]
verification_surface_globs = [".harness/**", ".github/workflows/**", "Cargo.toml", "Cargo.lock", "scripts/**", "docs/repository-split/**"]
generated_path_globs = ["target/**"]
selection_fallback_ids = []

[required_risk_categories]
